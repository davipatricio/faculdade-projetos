```algoritmo CriarListaDeIntervalo( min, max )
    lista ← ∅
    para i = min até max faça
        adiciona i à lista
    fim-para
    retorne lista
fim-algoritmo

algoritmo Principal()
    lista ← CriarListaDeIntervalo(1, 10)
    imprima "A lista:", lista
fim-algoritmo
```